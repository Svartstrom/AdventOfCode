pipeline {
    agent any

    stages {
        stage('Test') {
            steps {

                    echo 'Hello World'
                    //sh 'python3 main.py'
                    sh 'ls -a'
                    sh 'pytest 2023/ --junitxml=result.xml'

	        }
        //}
        //when {
            //expression {currentBuild.result != 'UNSTABLE'}
            //} steps {   
            stage('Build') {
                steps {
                    echo 'Hello from builder'
                    //sh 'python3 main.py'
                    sh 'ls -a'
                    //sh 'pytest 2023/ --junitxml=result.xml'
                }
            }
        //}
    }
}