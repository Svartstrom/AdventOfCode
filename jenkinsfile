pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                //sh 'python3 main.py'
                sh 'ls -a'
                sh 'pytest 2023/' --junitxml=.
	        }
        }
    }
}
